# S2 完了 → 次セッションは S3 から（2026-08-21）

## どこまで終わったか

- **S1（Phase 0）完了**: pnpm 切替（T01/T02）・規約移植（T03/T04）・Welcome の dayKey バグ修正（T05）
- **S2（Phase 1）完了**: packages/core 切り出し（T06）・packages/contracts の 33 コマンド契約（T07/T08/T11）・IPC 両端の型付き化と any 全廃（T09/T10）・ゲート（T12）
- ブランチ: `feat/rearchitecture`（worktree `.claude/worktrees/rearchitecture`）。**未 push**（push と PR 作成はまひろ確認待ち）

## 次の一手（S3: バックエンドのオニオン化）

1. [docs/plans/rebuild-sessions.md](../docs/plans/rebuild-sessions.md) の S3（T13〜T19）を読む
2. T13: `apps/desktop` を作り electron/ の中身を src/{domain,app,infra,presentation} に振り分ける（移動だけのコミットにする）
3. T14 以降: mutations の純関数化 → Port 注入 → ユースケーステスト

S2 で run() は既にハンドラ表（コマンド 1 つ = 関数 1 つ、契約型付き）になっているので、T15 は「関数を app/ へ移して Port を注入する」作業になる（switch の解体は済んでいる）。

## 注意

- pnpm 11 のビルド許可は `pnpm-workspace.yaml` の `allowBuilds`（旧 onlyBuiltDependencies は無視される）
- packages を触ったら必ず `pnpm build:packages` が先（typecheck / test / build スクリプトには組み込み済み）
- e2e は 21 チェック（契約拒否 2 本を含む）。`waitReady()` を消すと起動競合で flaky に戻る
- CLAUDE.local.md の「壊してはいけないもの」は S7 で新構造に書き直すまで旧パス表記のまま（`shared/engine.ts` → 実体は `packages/core/src/engine.ts`）

## ゲートの状態（このセッション最終）

`pnpm typecheck && pnpm test && node scripts/e2e.mjs` = 全緑（test 18 件 / e2e 21/21 PASS）
