# S3 完了 → 次セッションは S4 から（2026-08-21）

## どこまで終わったか

- **S3（Phase 2: バックエンドのオニオン化）完了**: T13 移動（オニオン配置）→ T14 task-ops 純関数化（TDD）→ T15/T16/T17 ユースケース抽出・IPC ルータ・infra 分割 → T18 ユースケース網羅テスト → T19 ゲート
- 現在の層: `apps/desktop/src/{domain,app,infra,presentation}`。app と domain に electron の import は 0 件（grep で機械確認済み）
- テスト 35 件（engine 12 / contracts 6 / task-ops 9 / handlers 8）・e2e 21/21

## 次の一手（S4: renderer の構造移動と重複統合）

1. [docs/plans/rebuild-sessions.md](../docs/plans/rebuild-sessions.md) の S4（T20〜T24）を読む
2. **T25 の基準スクショを先に撮る**（S5 の比較素材。S4 で見た目が変わらないことの証明にも使えるので、T20 の前に `node scripts/shoot.cjs` で 6 窓の PNG を確保しておくのが安全）
3. T20: `src/` → `apps/renderer/` へ移動し features/ 構造へ。vite.config・index.html・tsconfig・scripts の参照追従を忘れない（T13 と同じ「移動だけのコミット」方式）

## 注意

- ユースケースに触るときは `apps/desktop/tests/handlers.test.ts` が檻。コマンドを増やす場合は contracts → handlers → sample args の 3 点セット（テスト 1 と 2 が件数と返り値で落として教えてくれる）
- FakeCtx の時計は `advance(ms)` で進める。実時間 `Date.now()` はユースケース側では `ctx.now()` 経由のみ
- 旧 `quitting` フラグは読み手がいなかったので廃止済み（復活させない）

## ゲートの状態（このセッション最終）

`pnpm typecheck && pnpm test && node scripts/e2e.mjs` = 全緑（test 35 件 / e2e 21/21 PASS）
