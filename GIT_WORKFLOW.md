# Git / GitHub の基本的な操作フロー

## コミット＆プッシュ ≠ プルリクエスト

プッシュしても**自動では PR になりません**。

```
プッシュ = 書類を GitHub という「倉庫」に運び込んだだけ
PR      = 「この書類をメインの棚に入れてください」という依頼状を出す
```

倉庫に書類があるだけでは誰も気づかない。依頼状（PR）を出して初めて「これ確認してください」と伝わります。

---

## コマンドで見る違い

```bash
# これは書類を倉庫に運んだだけ。PR にはならない
git push origin my-branch

# これが依頼状。PR が作られる（gh CLI を使う場合）
gh pr create --title "〇〇機能の追加" --body "説明"
```

`git push` だけやって「PR 出しました」は通じません。必ず `gh pr create` という**別のコマンド**が必要です。

---

## なんで分かれているのか

プッシュしたブランチはそのまま作業を続けられます。追加のコミットをプッシュすれば、同じ PR に自動で反映されます。先に PR だけ作って「まだ作業中（Draft）」と表示することもできます。

---

## PR はどの時点の変更を見せるのか

PR が見せるのは、**「この作業ブランチ全体と main ブランチの差分」**です。

```
main ブランチ ────●────●────●────●
                              \
作業ブランチ                     ●────●────●
                                ↑
                         PR は「この3つのコミットの差分全部」を見せる
```

---

## よくある質問

| 質問 | 答え |
|------|------|
| PR はどの時点の変更を見せる？ | 作業ブランチの**全コミットまとめて**。main との差分 |
| ブランチ全体が PR になる？ | **はい**。作業ブランチの内容がすべて PR の対象になる |
| PR ごとに作業ブランチが必要？ | **はい**。1つの PR = 1つの作業ブランチ。これが基本ルール |

---

## 全体の流れ

```
1. 作業ブランチを作る
     ↓
2. コードを書いてコミットする
     ↓
3. プッシュする（GitHub にアップロード）
     ↓
4. プルリクエストを作る（ここが別ステップ！）
     ↓
5. レビュー → マージ
```

---

## White Box でのルール

- **1 セッション（再編中）／1 story（機能開発）= 1 ブランチ = 1 PR**。別の仕事は別ブランチで
- ブランチ名は `feat/…`・`fix/…`・`docs/…` のように内容が一目で分かる形にする
- **マージ前のゲート**: `pnpm typecheck && pnpm test && node scripts/e2e.mjs` が全緑であること。見た目を変えたときは実際に描画した PNG を見る（[docs/verification.md](docs/verification.md)）
- コミットはタスク単位で刻む。コミットメッセージは日本語で「何を・なぜ」を書く（コードを見なくても設計レビューできる粒度）
- TDD の残骸（skip されたテスト）・デッドコード・不要ファイルを PR に含めない
- 機能追加はまず [docs/stories/](docs/stories/) に story を書いてから実装する。セッションを跨ぐ作業は [tasks/](tasks/) に handover を残す
