# White Box — プロジェクト固有の前提

このファイルは、次にこのコードを触る人が知らないと壊す前提だけを持つ。

## この製品が守るもの（迷ったらここへ戻る）

- 真の進捗を、低摩擦で、歪みなく記録する。責めないが、曖昧な逃避は放置しない。
- **無駄な摩擦は徹底的に消す。必要な摩擦（今から何をやるか決める／どこまで進んだか確認する／止まっている重要タスクをどうするか決める）からは逃がさない。**
- タスクの粒度は人間に委ねる。分解は可能にするが義務にしない。
- 進捗は人間が宣言する値。子タスクや実績時間から自動計算しない。
- 仕事の階層（Project → Task → Subtask）と時間の階層（日 → セッション → 区間）を一対一に対応させない。

## 実装上の不変条件

- **セッションの真実はメインプロセスが持つ。** タイマーをレンダラに置かない（ウィンドウを閉じても計測が続く必要がある）。
- **実作業時間 = 経過 − 一時停止の重なり。** この計算は `shared/engine.ts` の純関数だけが行い、`tests/engine.test.ts` が守る。
- **呼び出し元のウィンドウを閉じるコマンドは `closeLater()` を使う（150ms 待ってから閉じる）。** 先に返事を返さないと呼び出し側の `await` が永久に返らない。`setImmediate` では IPC の返事の送信と競合し、ソース実行では通るのにパッケージ版で止まった。
- **preload は `.cjs` のまま置く。** package.json が `type: module` なので `.js` にすると読み込みに失敗する。
- **縦並び（flex column）の箱の中身には `flex: none` を効かせる。** 効いていないと中身が高さ 0 に潰れて文字が消える（開始画面・満了ポップアップ・詳細パネルで実際に起きた）。
- タスクを削除してもセッションの記録は消さない。記録側は「（削除されたタスク）」と表示する。

## 見た目の方針

「よく晴れた午後のカフェ」。地はクラフト紙のクリーム、作業面は紙の白、差し色はハウスグリーン `#00704A` 一点、
超過と注意だけキャラメル。構造（面・枠）は Notion のように控えめで角丸は小さく、操作するもの（ボタン・チップ）だけ丸くする。
色は必ず `src/styles/tokens.css` のトークン経由で使う。
書体は Windows 標準のみ（Segoe UI Variable / Georgia / Yu Gothic UI）でネット依存ゼロ。

## 検証の道具

| 目的 | コマンド |
| --- | --- |
| 時間計算の単体テスト | `npm test` |
| 実アプリを起動して IPC 経由の通し確認 | `node scripts/e2e.mjs` |
| 配布物そのものの通し確認 | `npm run pack` → `WHITEBOX_EXE="release/White Box/White Box.exe" node scripts/e2e.mjs` |
| 全画面のスクリーンショット | `node scripts/seed-demo.mjs .demo` → `electron scripts/shoot.cjs .demo <出力先>` |

- **撮影台（shoot.cjs）は非表示ウィンドウを `setOpacity(0)` + `showInactive()` で合成させている。** 隠したままだと DOM を変えても再合成されず、最初のフレームが撮れる（＝検証器が嘘をつく）。
- **e2e のデバッグポートは使用中でないことを確かめてから使う。** 他の Electron アプリが同じポートを掴んでいると、そちらのデバッガに繋がる。

## 配布

- `npm run pack` は electron の配布ツリーをコピーして `resources/app` を置き、`rcedit` でアイコンと製品名を焼くだけ（`scripts/pack.mjs`）。
- **electron-builder（`npm run dist`）は使えないことがある。** `winCodeSign` の展開にシンボリックリンク作成の権限が要り、開発者モードでない Windows では止まる。だから自前の pack を主経路にしている。
- `fs.cpSync` は electron の配布ツリー（数千ファイル）で落ちたので、コピーは `robocopy` を使う。
- ショートカットは `scripts/make-shortcuts.ps1`（**ASCII のみ**。日本語を入れると PS 5.1 が化けてコードごと壊れる）。デスクトップの場所は `[Environment]::GetFolderPath('Desktop')` で実行時に解決する。

## 現在地

Phase 1（MVP）完了。仕様書の Phase 2 以降は未着手。**ここで機能追加を止めて日常投入し、実使用で出た問題を Phase 2 に反映する**（仕様書 §「MVP完成条件」の開発原則）。
